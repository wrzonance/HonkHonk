# Rust
target/
Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*~
.helix/
.zed/
.aider*
.claude/

# OS
.DS_Store
Thumbs.db
.directory

# Environment / Secrets
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.secret

# Credentials / PII
credentials.json
secrets.json
token.json
*.credential
auth.json

# Debug / profiling
perf.data
perf.data.old
flamegraph.svg
*.profraw
*.profdata
callgrind.out.*
cachegrind.out.*

# Coverage
tarpaulin-report.html
lcov.info
coverage/

# Flatpak build
.flatpak-builder/
builddir/
repo/
*.flatpak

# AppImage build
*.AppImage
AppDir/

# Package build artifacts
*.deb
*.rpm
*.pkg.tar.*
*.snap

# Test fixtures — small generated files, committed for reproducible tests
# (regenerate via: ffmpeg -f lavfi -i "sine=frequency=440:duration=1:sample_rate=48000" ...)

# User sound library (never commit user audio)
sounds/
